---
import { Image } from 'astro:assets';

export interface Props {
  imageUrl: string;
  description: string;
  imageWidth: number;
  imageHeight: number;
}

const {
  imageUrl, 
  description, 
  imageWidth,
  imageHeight,
} = Astro.props
---

<article
  class="flex rounded-sm shadow-md overflow-hidden
    transition-shadow duration-300 cursor-pointer
    hover:border-gray-700 justify-center aspect-square
    shrink-0 w-full"
  data-trigger="view-image"
  data-imageurl={imageUrl}
  data-imagewidth={imageWidth}
  data-imageheight={imageHeight}
  data-description={description}
>
  <Image
    class={"object-contain transition-transform duration-300"}
    src={imageUrl}
    alt={description}
    width={imageWidth}
    height={imageHeight}
    format="webp"
    loading="lazy"
  />
</article>
